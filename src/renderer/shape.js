/* 线 */
export const line = (cntext, options) => ({});
/* 圆 */
export const circle = (cntext, options) => ({});
export const text = (cntext, options) => ({});
export const rect = (cntext, options) => ({});
export const path = (cntext, options) => ({});
export const ring = (cntext, options) => ({});
